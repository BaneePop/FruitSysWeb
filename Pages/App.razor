@using Microsoft.AspNetCore.Components.Web
@using ApexCharts
@using FruitSysWeb.Components.Charts
@using Microsoft.AspNetCore.Components.Routing
@using FruitSysWeb.Shared

<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(NavMenu)" />
    </Found>
    <NotFound>
        <LayoutView Layout="@typeof(NavMenu)">
            <div class="container mt-5">
                <h1>Stranica nije pronađena</h1>
                <p>Tražena stranica ne postoji.</p>
                <a href="/" class="btn btn-primary">Nazad na početnu</a>
            </div>
        </LayoutView>
    </NotFound>
</Router>